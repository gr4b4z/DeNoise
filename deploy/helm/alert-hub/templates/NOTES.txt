Alert Hub {{ include "alert-hub.tag" . }} deployed as release {{ .Release.Name }}.

Hosts: {{ include "alert-hub.fullname" . }}-api, -ingest, -workers (roles: {{ .Values.workers.roles }}).
Readiness: GET /healthz/ready on each service.
Migrations ran as a pre-upgrade Job; check `kubectl logs job/{{ include "alert-hub.fullname" . }}-migrate` if the release stalled.
